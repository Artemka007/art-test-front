export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortBy {
  NAME = 'name',
  TYPE = 'type',
  INDUSTRY = 'industry',
}

export const SORT_BY = [SortBy.NAME, SortBy.TYPE, SortBy.INDUSTRY];
